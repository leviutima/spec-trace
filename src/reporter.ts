/**
 * Vitest custom reporter — implemented in Phase 2.
 * Will write `.spec-trace/results.json` capturing the full describe/it
 * name chain and the real test status (passed/failed/skipped/todo).
 */
export class SpecTraceReporter {}
