## REQ-001 — Cart accepts a positive quantity

**When** a positive quantity is submitted, **the system shall** accept it.

## REQ-002 — Cart rejects a negative quantity

**When** a negative quantity is submitted, **the system shall** reject it with an error.
